import styled from 'styled-components';

export const Logo = styled.header`
  display: none;
  @media (min-width: 900px) {
    display: block;
    margin-left: -8.5rem;
  }
  svg {
    font-size: 5rem;
  }
`;